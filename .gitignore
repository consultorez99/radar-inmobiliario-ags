# Insumos crudos pesados (shapefiles, PDFs, JPGs de planos) — no se versionan.
# Se regeneran con scripts/build_*.py; ver README.md para las fuentes.
data/raw/

# Entorno virtual de Python
.venv/
__pycache__/
*.pyc

# Config local del preview (rutas absolutas de esta máquina)
.claude/

# Sistema
.DS_Store

# Exportación XLSX para análisis: incluye tablas del estudio de mercado de
# terceros (Softec) que el despacho adquirió — no es dato abierto, no se versiona.
Radar_Inmobiliario_Ags_datos.xlsx

# Dependencias de los tests (npm)
node_modules/
